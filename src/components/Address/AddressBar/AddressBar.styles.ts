import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    addressBarContainer: {
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center',
        margin: 5
    }
})

export default styles